# Auto-generated by bootstrap_localstack.sh
# Lambda Function URL: http://tyc5dw81w6esofbexrbpkis4eyr5b82p.lambda-url.ap-northeast-1.localhost.localstack.cloud:4566/
server {
    listen 80;
    server_name _;

    # プリフライト (OPTIONS) は Nginx で即時応答し Lambda に転送しない
    location / {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header Access-Control-Allow-Headers 'Content-Type, Authorization' always;
            add_header Access-Control-Max-Age 86400;
            return 204;
        }

        resolver 127.0.0.11 valid=10s;
        set $upstream http://data-collector-localstack:4566;
        proxy_pass $upstream;
        proxy_set_header Host tyc5dw81w6esofbexrbpkis4eyr5b82p.lambda-url.ap-northeast-1.localhost.localstack.cloud:4566;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Origin "";
        proxy_set_header Referer "";
        proxy_read_timeout 30s;

        # Lambda からのレスポンスに CORS ヘッダーを付与（リクエスト側には送らない）
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        add_header Access-Control-Allow-Origin * always;
        add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header Access-Control-Allow-Headers 'Content-Type, Authorization' always;
    }
}
